# Intentionally empty
Place your self-contained GLB files here. No dinosaur, bug, fossil, plant, microscope,
rock, diorama or other specimen models are provided with this architectural edition.
Set a slot's `url` in ../exhibits.json to `models/D01.glb`, for example.
A local browser file preview does not copy files into this directory.
