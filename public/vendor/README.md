The engine is installed by `npm install`, whose postinstall step copies the
pinned Three.js package into this folder as `three/`. The delivered source
archive does not include a downloaded Three.js package. Without local engine
files, the browser uses an internet-dependent CDN fallback.
